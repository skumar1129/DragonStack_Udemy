const SECONDS = 1000;
const MINUTES = 60 * SECONDS;
const HOURS = 60 * MINUTES;
const DAYS = HOURS * 24;

const REFRESH_RATE = 5; //units

module.exports = {
  SECONDS,
  MINUTES,
  HOURS,
  DAYS,
  REFRESH_RATE
};
